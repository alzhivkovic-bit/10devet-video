<section class="about">
	<div class="about-intro">
		<p class="eyebrow">10DEVET</p>

		<h2>FUDBAL NIJE SAMO REZULTAT.</h2>

		<p class="intro-text">
			Iza svakog igrača, transfera, utakmice i rivalstva postoji priča.
			10devet ih istražuje i pretvara u video.
		</p>
	</div>

	<div class="about-bottom">
		<div class="about-person">
			<p class="eyebrow">KO STOJI IZA 10DEVET</p>

			<h3>ALEKSANDAR ŽIVKOVIĆ</h3>

			<p>
				10devet je projekat koji spaja ljubav prema fudbalu sa
				video produkcijom, motion designom i vizuelnim storytellingom.
			</p>

			<a href="/o-nama" class="about-link">
				VIŠE O 10DEVET <span>→</span>
			</a>
		</div>

		<div class="skills">
			<span>VIDEO EDITING</span>
			<span>MOTION DESIGN</span>
			<span>VISUAL STORYTELLING</span>
			<span>GRAPHIC DESIGN</span>
			<span>SOUND DESIGN</span>
			<span>DOCUMENTARY PRODUCTION</span>
		</div>
	</div>
</section>

<style>
	.about {
		padding: 140px 5vw;
		background: #0c0c0c;
	}

	.about-intro {
		max-width: 1100px;
	}

	.eyebrow {
		margin: 0 0 20px;
		color: var(--red);
		font-size: 11px;
		font-weight: 700;
		letter-spacing: 0.16em;
	}

	h2 {
		margin: 0;
		font-family: 'Bebas Neue', sans-serif;
		font-size: clamp(60px, 8vw, 120px);
		font-weight: 400;
		line-height: 0.88;
		letter-spacing: 0;
	}

	.intro-text {
		max-width: 620px;
		margin: 40px 0 0;
		color: rgba(224, 214, 205, 0.68);
		font-size: 20px;
		line-height: 1.6;
	}

	.about-bottom {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 10vw;
		margin-top: 120px;
		padding-top: 40px;
		border-top: 1px solid rgba(224, 214, 205, 0.12);
	}

	.about-person {
		max-width: 520px;
	}

	h3 {
		margin: 0;
		font-family: 'Bebas Neue', sans-serif;
		font-size: clamp(38px, 4vw, 58px);
		font-weight: 400;
		line-height: 0.95;
	}

	.about-person > p:not(.eyebrow) {
		margin: 24px 0 30px;
		color: rgba(224, 214, 205, 0.62);
		font-size: 15px;
		line-height: 1.7;
	}

	.about-link {
		display: inline-flex;
		align-items: center;
		gap: 14px;
		padding-bottom: 8px;
		border-bottom: 1px solid var(--red);
		font-size: 12px;
		font-weight: 700;
		letter-spacing: 0.08em;
		transition: gap 0.25s ease;
	}

	.about-link:hover {
		gap: 22px;
	}

	.skills {
		display: grid;
		grid-template-columns: 1fr 1fr;
		align-content: start;
		border-top: 1px solid rgba(224, 214, 205, 0.12);
	}

	.skills span {
		padding: 18px 0;
		border-bottom: 1px solid rgba(224, 214, 205, 0.12);
		color: rgba(224, 214, 205, 0.65);
		font-size: 10px;
		font-weight: 700;
		letter-spacing: 0.1em;
	}

	@media (max-width: 800px) {
		.about {
			padding: 90px 5vw;
		}

		.intro-text {
			font-size: 17px;
		}

		.about-bottom {
			grid-template-columns: 1fr;
			gap: 60px;
			margin-top: 80px;
		}

		.skills {
			grid-template-columns: 1fr;
		}
	}
</style>